#include <stdio.h>
#include <stdlib.h>

int main(void){
	int contador;
	
	contador = (contador / 1 );
	for (contador = 1; contador <= 100; contador++)
	printf("contando: %d\n", contador);
	
	printf("\n");
	system("pause");
	return(0);
}
