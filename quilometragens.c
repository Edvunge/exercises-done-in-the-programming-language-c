#include<stdio.h>
#include<stdlib.h>
#include<conio.h>
#include<string.h>

/* Função :
// Autor :
// Data : 17/11/2017
*/  
int main (void){

float cont, automovel, quilometros_dirigidos, litros_utilizados;

cont = (0);
printf ( " quantos quilometros foram dirigidos:? " );
scanf ("%f" , &quilometros_dirigidos);
printf ( " quantos litros foram utilizados:? " );
scanf ( "%f", &litros_utilizados );
automovel = ( ( quilometros_dirigidos + litros_utilizados ) * 1,5 );

      if ( automovel < 8 ){
      printf(" ' Alerta Nivel Critico ' ");
      
                 if( automovel == 8  ||  automovel >= 10 );
                     printf(" ' Alerta Nivel Complicado ' ");
                 else
                      if( automovel <= 10 );
                      escreval("");
                      
					  }
                    	else printf( automovel %f );

system( " pause " );
return = ( 0 );
}
